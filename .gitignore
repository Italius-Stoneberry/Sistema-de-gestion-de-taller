# Secretos: nunca subir
.env
*.env
!.env.example

# Backups de la base (no van al repo)
backups/

# Dependencias / build
node_modules/
*.log

# Carpetas de trabajo que no van al repo
basura/
TLLer/

# Basura del SO
.DS_Store
Thumbs.db
*.tmp
.~lock.*
